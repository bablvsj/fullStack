import axios, {
    AxiosResponse,
    AxiosError,
    AxiosRequestConfig,
    AxiosInstance,
  } from "axios";
  
  const config = {
      baseUrl: import.meta.env.VITE_API_URL,
      timeout:Resu
  
  }
  